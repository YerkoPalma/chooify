/* choo-view */

`
<main onload=${(e) => send('init')}>
  <h1 id="msg"></h1>
</main>
`

/* choo-model */
{
  effects: {
    init: (data, state, send, done) => {
      document.getElementById('msg').innerHtml = 'Hello world'
    }
  }
}
